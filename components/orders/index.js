/**
 * Placeholder for PartnerLocationSelect component
 * This serves as a temporary workaround for imports until the actual component is available
 */

import { PartnerLocationComponent } from './PartnerLocationComponent';

// Export the PartnerLocationComponent as PartnerLocationSelect
// This ensures EnhancedCreateOrderForm uses our fixed component
export const PartnerLocationSelect = PartnerLocationComponent;

// Export other components as needed 